# QtCreator by default creates build directories starting with 'build',
# and we have a default (empty) build directory 'build'
/build*/

# QtCreator saves user specific project configuration in *.user
/CMakeLists.txt.user
# avoid to add QtCreators autosave by accident
*.autosave

# doc/html/* is generated by doxygen and shouldn't disturb git
/doc/html/

# generated by bin/run-doxygen
/doc/doxygen/project-number.dox

# ignore some backup files students might add otherwise
*~
*.bak
*.saved
*.old
*.orig
*.swp
*.autosave
